- name: Install Docker + Compose
  apt:
    name:
      - docker.io
      - docker-compose-plugin
    state: present
    update_cache: yes

- name: Add user to docker group
  user:
    name: "{{ node_user }}"
    groups: docker
    append: yes